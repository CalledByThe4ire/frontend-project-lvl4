doctype html
html.h-100(lang="en")
  head
    title= Title
    link(href=assetPath('main.css') rel="stylesheet")
    script.
      var gon = !{JSON.stringify(gon)};
  body.d-flex.flex-column.h-100
    .container-fluid.h-100.overflow-hidden.p-0
      #app.h-100.d-flex.flex-column
      // block content
    script(src=assetPath('main.js'))

