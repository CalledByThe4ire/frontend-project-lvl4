const initialState = {
  channels: [],
  currentChannelId: null,
};

const channelsReducer = (state, action) => {
  switch (action.type) {
    default:
      return initialState;
  }
};

export default channelsReducer;
